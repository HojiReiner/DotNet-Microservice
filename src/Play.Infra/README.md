# Play.Infra

**Files related to project infrastructure**

## Usefull commands
```pwsh
#See docker process
docker ps

#Stop a docker process
docker stop <name>

#Initiate a docker compose
docker-compose up -d
```

